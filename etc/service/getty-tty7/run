#!/bin/sh
# -P makes busybox getty a session leader (setsid) for its tty, so it
# properly owns the controlling terminal instead of inheriting lksys's.
clear
exec chpst -P busybox getty 38400 tty7 linux
